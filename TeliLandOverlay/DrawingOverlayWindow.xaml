<Window x:Class="TeliLandOverlay.DrawingOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TeliLand Drawing Overlay"
        Width="800"
        Height="600"
        Background="#01000000"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual">
    <Grid x:Name="InteractionSurface"
          Background="#01000000"
          Cursor="Pen"
          MouseLeftButtonDown="InteractionSurface_OnMouseLeftButtonDown"
          MouseMove="InteractionSurface_OnMouseMove"
          MouseLeftButtonUp="InteractionSurface_OnMouseLeftButtonUp">
        <Canvas x:Name="DrawingCanvas"
                IsHitTestVisible="False" />
    </Grid>
</Window>
